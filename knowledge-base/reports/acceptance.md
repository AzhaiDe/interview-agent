# 知识库验收报告

## 自动化验收结果

执行时间：2026-08-12

```text
npm run kb:build     PASS
npm run kb:validate  PASS
```

三岗位均已达到 PRD 的数量目标：30 能力、150 概念、300 问题、600 追问、200 误区、100 故障、30×5 评分锚点。所有记录均有稳定 ID、岗位归属、来源 ID、状态和时间戳；重复 ID 与缺失溯源校验通过。

## 必须由专家完成的验收

当前不能把所有生成记录标为 `verified`，原因是论坛面经是候选证据，且本地没有三岗位专家的逐条盲审结果。发布前必须完成：

1. 每岗位至少两名专家对技术事实、问题难度、追问信息增益和评分锚点独立标注；
2. 对低于加权 Kappa 0.70 的条目返工；
3. 用官方文档/论文/开源项目文档核对关键结论；
4. 清理转载、营销稿、AI 二次整理和过时版本；
5. 将通过条目的 `status` 从 `pending_expert_review`/`candidate` 升级为 `verified`；
6. 运行产品离线评测，确认引用可追溯率、事实正确率、追问有效率和评分一致性。

因此本报告的准确表述是：**结构、数量、溯源和自动校验已完成；内容的专家最终验收尚未完成**。这是为了避免把自动派生素材误当作真实面试标准。

## 小红书本轮追加结果

本轮已在登录态下完成三组检索，并将 11 条小红书候选来源写入 `seed/sources.json`：后端 3 条、AI/RAG/Agent 4 条、前端 4 条。来源平台总数已扩展为 nowcoder、zhihu、juejin、official_docs、xiaohongshu 五类；本次构建记录 26 条来源。

检索到的高信号主题包括：多轮后端项目拷打、Java/MySQL/Redis 八股、Agent 岗三轮技术面、RAG/Agent 高频题、AI 应用开发、AI 前端、SSE/流式交互、前端性能和手撕题。

## 后续小红书追加流程

登录后只需在 `seed/sources.json` 增加来源记录，字段至少包括：平台、公开 URL、标题、岗位、来源类型、采集时间、版权状态和主题标签。然后重新执行：

```bash
npm run kb:build
npm run kb:validate
```

小红书帖子中的昵称、头像、联系方式、简历截图和整篇原文不进入知识库；只保留可追溯链接、短事实摘要和经过审校的结构化实体。
